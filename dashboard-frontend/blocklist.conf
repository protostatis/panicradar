# Blocked scanner IPs — updated 2026-02-09
# Volume-mounted so it can be updated without rebuilding the image.
# After editing, reload nginx: docker exec crypto-frontend nginx -s reload

deny 172.233.178.66;   # Nmap scanner (Linode)
deny 4.188.251.0;      # Automated scanner (Azure)
deny 217.154.69.208;   # Coordinated scanner
deny 192.3.90.15;      # Coordinated scanner
deny 173.212.239.181;  # Coordinated scanner
deny 185.211.94.76;    # Coordinated scanner
deny 177.70.2.220;     # Coordinated scanner
deny 144.79.133.252;   # Coordinated scanner
deny 185.16.39.146;    # Heavy scanner
